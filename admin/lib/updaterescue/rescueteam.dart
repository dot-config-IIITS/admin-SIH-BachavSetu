import 'package:flutter/material.dart';

class ManageRescueTeamPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Manage Rescue Team'),
      ),
      body: Center(
        child: Text('Update Rescue Team Coming Soon!'),
      ),
    );
  }
}
